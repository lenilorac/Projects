//Leo and Caroline
/*
class graph
{
  ArrayList<Bar> bars;

  graph(ArrayList<Integer> array)
  {
    bars = new ArrayList<Bar>();
    for(int i = 1;i < array.size(); i++)
    {
      Bar bar = new Bar(array.get(i), i);
      bars.add(bar);
    }
  }
  
  void draw()
  {
    
    line(200, 100, 200, 600);
    line(200, 600, 800, 600); 
    for(int i = 0; i < bars.size(); i++)
    {
      Bar bar = bars.get(i);
      bar.draw();
    }
  }
}
*/